name: managment-center-build
permissions:
  packages: write
  contents: write
on:
  # Triggered when code is pushed to the repository,
  # specifically when changes occur within pkg/baccarat-managment-center directory.
  push:
    branches:
      - main
    # paths:
    #   - "pkg/baccarat-managment-center/**"
  # Triggered when a new GitHub Release is created and published.
  release:
    types: [published]
  # Manual trigger for rollback to specific release or redeploy latest
  workflow_dispatch:
    inputs:
      version:
        default: latest
        description: Tag you want to release.
        required: true

jobs:
  push_to_registry:
    runs-on: ubuntu-22.04
    steps:
      # Checkout latest or specific tag
      - name: checkout
        # if: ${{ github.event.inputs.version == '' || github.event.inputs.version == 'latest' }}
        uses: actions/checkout@v3
      - name: checkout tag
        if: ${{ github.event.inputs.version != '' && github.event.inputs.version != 'latest' }}
        uses: actions/checkout@v3
        with:
          ref: refs/tags/${{ github.event.inputs.version }}

      # Assign environment variables used in subsequent steps
      - name: Env variable assignment
        run: echo "image_repository_name=$(echo $GITHUB_WORKFLOW | sed 's/-build$//')" >>${GITHUB_ENV}
      # TAG_NAME defaults to 'latest' if not a release or manual deployment
      - name: Assign version
        run: |
          docker build . -t passon/baccarat-managment-server --progress=plain -f ./pkg/baccarat-managment-center/Dockerfile
          docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin ${{ secrets.DOCKERHUB_PASSWORD }}
          docker push passon/baccarat-managment-server
          docker logout
          