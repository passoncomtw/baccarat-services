import { connect } from "react-redux";
import TableScreen from "./view";

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(TableScreen);
