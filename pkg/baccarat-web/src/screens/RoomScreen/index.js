import { connect } from "react-redux";
import RoomScreen from "./view";

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(RoomScreen);
